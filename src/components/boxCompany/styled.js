import styled from "styled-components";

export const BoxCompany = styled.div`

    display: flex;
    align-items: center;
    flex-direction: column;
    text-align: center;

    padding: 20px;

    border-radius: 26px;
    background: linear-gradient(315deg, #c3c3c3, #e8e8e8);
    box-shadow: 0px 0px 14px 2px #b4b4b4, 0px 1px 14px #fefefe;
`

export const Title = styled.h2`
    font-size: 1.5rem;
    padding: 0 0 8px 0;
    font-weight: 900;
`
export const Subtitle = styled.h3`
    font-size: 1.2rem;
    padding: 0 0 12px 0;
    font-weight: 600;
`